<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enes.notlar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/defter"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/defter_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Enes">
        <activity android:name="com.enes.notlar.MainGecis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.enes.notlar.detaylariGoster" />
        <activity android:name="com.enes.notlar.detaysayfasii" />
        <activity android:name="com.enes.notlar.MainActivity">

        </activity>
    </application>

</manifest>